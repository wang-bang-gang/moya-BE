spring:
  datasource:
    url: jdbc:mysql://moya-dev.cj8yo000ag6t.ap-northeast-2.rds.amazonaws.com:3306/moya?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: moya
    password: moya1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true

  app:
    cdn:
      audio-base-url: https://cdn.example.com/audio

  web:
    cors:
      allowed-origins:
        - "http://localhost:3000"
        - "http://localhost:3001"
        - "https://d2ji3w396izzkh.cloudfront.net/"
        - "http://43.200.5.81/"
      allowed-methods:
        - GET
        - POST
        - PUT
        - DELETE
        - PATCH
        - OPTIONS
      allowed-headers: "*"
      allow-credentials: true
      max-age: 3600